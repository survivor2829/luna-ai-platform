# Luna AI Platform - Environment Variables
# Copy this file to .env and fill in the values

# ===========================================
# REQUIRED - Must be set before starting
# ===========================================

# JWT Secret Key for authentication tokens
# Generate a secure random string, e.g.: openssl rand -hex 32
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this

# Default admin password (only used on first startup)
# After first startup, you can remove this
ADMIN_DEFAULT_PASSWORD=your-secure-admin-password

# ===========================================
# OPTIONAL - Database Configuration
# ===========================================

# Database URL (default: SQLite)
# For production, use PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/luna_ai
# DATABASE_URL=sqlite:///./luna_ai.db

# ===========================================
# OPTIONAL - Security Settings
# ===========================================

# CORS allowed origins (comma-separated)
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# SSL verification for Coze API
# Set to "true" in production, "false" only if Coze has certificate issues
# Default: false (for development compatibility)
COZE_SSL_VERIFY=false

# ===========================================
# NOTES
# ===========================================
# 1. Never commit .env file to version control
# 2. Use strong, unique values for JWT_SECRET_KEY
# 3. Change ADMIN_DEFAULT_PASSWORD after first login
